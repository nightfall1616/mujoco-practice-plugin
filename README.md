# mujoco-practice-plugin
Example plugin for MuJoCo. 
# MuJoCo Constant Force Plugin

An example MuJoCo engine plugin.

The plugin applies a constant force of:

(1, 0, 0)

to the attached body.

## Build

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

## Example XML

See example.xml.